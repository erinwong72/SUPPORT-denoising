# guide to using SUPPORT with preprocessing pipeline
**will be updated soon, does not reflect the current state of the repo!**
start with `preprocess_with_support.m` first:

functions:
- `generate_sessions_struct`: structure to navigate directories
    - select specific FOVs/slices to analyze 
- `pool_intens_analysis`:
- 

scripts:
- `support_analysis.m`: quantify and visualize improvement SUPPORT makes on metrics like SNR/preserving waveform, etc.
    - inform implementation choice of when in the pipeline to use SUPPORT (pre vs post motion correction)
- `simple_preprocess.m`:
- `batch_preprocess.m`:
